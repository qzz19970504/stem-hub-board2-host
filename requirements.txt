# stem-hub-board2-host 上位机 Python 依赖
# 安装: pip install -r requirements.txt
# 推荐使用清华源: pip install -i https://pypi.tuna.tsinghua.edu.cn/simple -r requirements.txt

# --- GUI 框架 ---
# PySide6 的 pip / conda 包不含 QtSerialPort (GPL 模块), 需要 Addons
PySide6>=6.8
PySide6-Addons>=6.8

# --- 测试 ---
pytest>=8.0
